apiVersion: v1
kind: Service
metadata:
  name: messaging-app-service
spec:
  selector:
    app: messaging-app  # both blue and green share this label
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
